# SHA1 IV ADRP+ADD xref scan

Target: vaddr 0x2db3d4 (SHA1 IV sequence). No ADRP+ADD sequences in executable segments resolved to this address.

Output list (if any): `analysis/sha1_iv_adrp_add_hits.txt`.
